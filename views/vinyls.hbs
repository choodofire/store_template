<div class="shop" id="shop">
        {{#if vinyls.length}}
                {{#each vinyls}}
                    <div class="shop__card-link">
                        <a href="/shop/{{_id}}">
                            <div>
                                <div class="shop__card">
                                    <div>
                                        <img src="/{{img}}" alt="{{title}}">
                                    </div>
                                    <div class="shop__card__description">
                                        <span>{{title}}</span>
                                        <p class="price">{{price}}</p>
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
                {{/each}}
        {{else}}
            <p>Пластинок нет</p>
        {{/if}}
</div>
